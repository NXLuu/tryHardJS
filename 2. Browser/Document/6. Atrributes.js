/* 
    Properties:
        1. Loại giá trị là bất kì giá trị nào
        2. Tên phân biệt chữ hoa chữ thường
    Attributes:
        1. loại là một string
        2. Tên không phân biệt chữa hoa
    - elem.hasAttribute(name) - Kiểm tra xem một attributes có tồn tại

    - elem.getAttribute(name) – trả về giá trị của Attributes.
    - elem.setAttribute(name, value) – set giá trị.
    - elem.removeAttribute(name) – to remove the attribute.
    - elem.attributes is a collection of all attributes.

*/

/* 
    data-nameAtrributes (id)
    let atrributes = id.dataset.nameAtrributes
*/